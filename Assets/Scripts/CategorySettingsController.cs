using UnityEngine;

/// <summary>
/// カレンダーのカテゴリー名エディタの開閉エントリ（Monthly/Weekly/Daily の歯車から呼ぶ）。
/// 本体パネルは次ステップで実装。今はスタブ（クリックでログのみ）。
/// </summary>
public static class CategorySettingsController
{
    public static void Toggle()
    {
        Debug.Log("[CategorySettings] 歯車クリック（スタブ）。カテゴリー名エディタは次ステップで実装します。");
    }
}